---
description: 
globs: 
alwaysApply: false
---
# 企业微信API更新流程指南

此指南帮助开发者在添加或更新企业微信API时遵循标准流程。

## 更新流程

更新企业微信API功能的标准流程如下：

1. 根据企业微信的API文档更新 ](mdc:docs/wedoc.md) 或其他相关文档文件
   - 用fetch mcp去读企业微信API文档，不要用搜索的方式去获取企业微信API文档内容
   - 参考示例：https://developer.work.weixin.qq.com/document/path/97460
   - 确保按照既有格式添加Models和API calls部分
    - 请求包体 -> Models 里面的请求类型
    - 返回示例 -> Models 里面的返回类型
    - 模型类型能用表格表示的都用表格表示，枚举类型用go代码表示
    - 根据 "请求地址" 生成api call名字 例如 "https://qyapi.weixin.qq.com/cgi-bin/wedoc/get_doc_base_info?access_token=ACCESS_TOKEN" -> api call name: execWedocGetDocBaseInfo

2. 在项目根目录执行代码生成命令 `go generate ./...`
   - 这将从markdown文档生成相应的Go代码文件(*.mdummy_fo.go](mdc:dummy_for_generate.go)
   - 也可以单独为某个文件生成代码，例如 `go run --tags sdkcodegen ./internal/sdkcodegen doc.md ./wedoc.md.go`

3. 在 [models.go](mdc:models.go) 中实现API调用的请求参数类型的bodyer接口
   - 不用动models.go里面其他的部分的代码和注释
   - **重要**：不要在models.go中手动添加结构体定义，这些应该由代码生成工具从markdown文档中生成
   - 只需实现请求结构体的bodyer接口和intoBody()方法即可
   ```go
   var _ bodyer = reqXXX{}
   
   func (x reqXXX) intoBody() ([]byte, error) {
       return marshalIntoJSONBody(x)
   }
   ```

4. 在 [wedoc.go](mdc:wedoc.go) (或相关功能文件) 中封装生成的API调用
   - 创建用户友好的公开方法
   - 封装底层execXxx方法
   - 提供清晰的参数文档和返回值说明

## 文件结构

- `docs/*.md`: API定义文档（按功能分类）
- `*.md.go`: 从markdown自动生成的Go代码
- `*.go`: 手动实现的API封装和扩展功能

## 命名规范

- API定义文档中的execXxx方法遵循统一命名模式
- Go封装方法使用清晰的领域术语，如CreateWedoc、RenameWedoc等

## 常见问题

- 如果生成代码与现有实现不兼容，检查markdown文档格式是否正确
- 确保所有请求类型都正确实现了bodyer接口
- 如果出现结构体重复定义错误，确保没有在models.go中手动添加已由生成代码提供的结构体
- 所有的数据模型应该在markdown文档中定义，然后由代码生成工具生成
